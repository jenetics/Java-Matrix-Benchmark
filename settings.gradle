include 'main'
include 'external:colt'
include 'external:commons-math'
include 'external:ejml'
include 'external:hipparchus'
include 'external:jama'
include 'external:jblas'
include 'external:la4j'
include 'external:lattices'
include 'external:mtj'
include 'external:ojalgo'
include 'external:parallelcolt'
include 'external:ujmp'

// the following algorithms require non-Maven dependencies
if( file('external/jeigen/libs/Jeigen-onefat.jar').exists()) {
    include 'external:jeigen'
} else {
    logger.warn("Skipping jeigen since local dependency can't be found")
}

// Compose builds.
if (file('../Lattices').exists()) {
    includeBuild('../Lattices') {
        dependencySubstitution {
            substitute module('io.jenetics:lattices') using project(':lattices')
        }
    }
}